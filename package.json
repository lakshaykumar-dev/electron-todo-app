{
  "name": "electron-todo-app",
  "version": "1.1.0",
  "description": "A sleek, modern desktop To-Do application built with Electron",
  "main": "src/main/main.js",
  "scripts": {
    "start": "electron .",
    "package:win": "electron-packager . \"TaskFlow\" --platform=win32 --arch=x64 --out=dist --overwrite --asar",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": [
    "electron",
    "todo",
    "desktop",
    "gui"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "electron": "^33.2.1",
    "electron-packager": "^17.1.2"
  }
}
